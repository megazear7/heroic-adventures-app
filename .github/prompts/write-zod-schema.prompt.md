---
description: Add a Zod schema for data validation.
name: write-zod-schema
---

Add a Zod schema for [data]. Reference the write-zod-schema skill. Define the schema in `src/shared/`, export it, and use `.parse()` to validate data at runtime.

Related Skills:
- write-zod-schema
